type Query {
  info: String!
  sales: [Sale!]!
  users: [User!]!
}

type Mutation {
    post(url: String!, description: String!): Sale!
}

type Sale {
    id: ID!
    name: String!
    desc: String!
    userId: Int!
}

type User {
    id: ID!
    name: String!
    sale: [Sale!]!
}
